name: realm_test
description: "A custom todo app with realm implementation"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  animated_snack_bar: ^0.4.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  provider: ^6.1.1
  realm: ^1.6.0

dev_dependencies:
  analyzer: 5.13.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
